const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const user = await prisma.usuario.findUnique({ where: { username: 'cristina' }, include: { rol: { include: { permisos: { include: { permiso: true } } } } } }); console.log(JSON.stringify(user, null, 2)); } catch(e) { console.error('Error in query:', e); } } main().finally(() => process.exit(0));
