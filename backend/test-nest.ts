import { NestFactory } from '@nestjs/core'; import { AppModule } from './src/app.module'; import { AuthService } from './src/auth/auth.service'; async function bootstrap() { const app = await NestFactory.createApplicationContext(AppModule); const auth = app.get(AuthService); try { await auth.login('cristina', 'padrepio_74'); } catch(e) { console.error('Login Error:', e); } await app.close(); } bootstrap();
